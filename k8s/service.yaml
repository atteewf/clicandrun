apiVersion: v1
kind: Service
metadata:
  name: clicandrun-backend
spec:
  selector:
    app: clicandrun-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort
